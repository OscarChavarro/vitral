import { Surface } from "./Surface.js";import { Ray } from "../element/Ray.js";import { RayHit } from "../element/RayHit.js";import { Vector3Dd } from "../../../common/linealAlgebra/Vector3Dd.js";
export interface TriangleMeshLike {getMinMax():Float64Array|number[];doIntersectionFirstHit(ray:Ray,hit:RayHit,triangleInformation?:Int32Array|number[]):boolean;doContainmentTest(p:Vector3Dd,tolerance:number):number;}
export class TriangleMeshGroup extends Surface<Ray,RayHit> {private meshes:TriangleMeshLike[]=[];private information:[number,number]=[-1,-1];public constructor(meshes?:TriangleMeshLike[]){super();if(meshes!==undefined)this.meshes=meshes;}public getMeshes(){return this.meshes;}public setMeshes(x:TriangleMeshLike[]){this.meshes=x;}public addMesh(x:TriangleMeshLike){this.meshes.push(x);}public getMeshAt(i:number){return this.meshes[i]!;}public getMinMax():Float64Array{const m=new Float64Array([Infinity,Infinity,Infinity,-Infinity,-Infinity,-Infinity]);for(const mesh of this.meshes){const x=mesh.getMinMax();for(let i=0;i<3;i++){m[i]=Math.min(m[i]!,x[i]!);m[i+3]=Math.max(m[i+3]!,x[i+3]!);}}return m;}public calculateMinMaxPositions():void{this.getMinMax();}public doIntersectionFirstHit(ray:Ray,out:RayHit):boolean{let best:RayHit|null=null,meshIndex=-1,triangle=-1;for(let i=0;i<this.meshes.length;i++){const hit=new RayHit(),info=new Int32Array(1);if(this.meshes[i]!.doIntersectionFirstHit(ray,hit,info)&&hit.ray()!==null&&(best===null||hit.hitDistance()<best.hitDistance())){best=new RayHit();best.clone(hit);meshIndex=i;triangle=info[0]!;}}if(best===null){this.information=[-1,-1];return false;}this.information=[meshIndex,triangle];out.clone(best);return true;}public override doExtraInformation(ray:Ray,t:number,out:RayHit):void{this.doIntersectionFirstHit(ray.withT(t),out);}public doIntersectionInformation():[number,number]{return [...this.information] as [number,number];}public override doContainmentTest(p:Vector3Dd,t:number):number{for(const mesh of this.meshes){const status=mesh.doContainmentTest(p,t);if(status!==TriangleMeshGroup.OUTSIDE)return status;}return TriangleMeshGroup.OUTSIDE;}public exportToTriangleMeshGroup(){return this;}public override toString(){return `TriangleMeshGroup < #Mesh: ${this.meshes.length} >`;}}
