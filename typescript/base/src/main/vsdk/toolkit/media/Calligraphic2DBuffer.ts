import { MediaEntity } from "./MediaEntity.js";import { Vector3Dd } from "../common/linealAlgebra/Vector3Dd.js";import { RGBImageUncompressed } from "./RGBImageUncompressed.js";import { RGBPixel } from "./RGBPixel.js";
export class Calligraphic2DBuffer extends MediaEntity {private lines:number[]=[];public init():void{this.lines=[];}public add2DLine(a:Vector3Dd,b:Vector3Dd):void;public add2DLine(x0:number,y0:number,x1:number,y1:number):void;public add2DLine(a:number|Vector3Dd,b:number|Vector3Dd,c?:number,d?:number):void{if(a instanceof Vector3Dd){this.add2DLine(a.x(),a.y(),(b as Vector3Dd).x(),(b as Vector3Dd).y());return;}this.lines.push(a,b as number,c!,d!,0,0,0,1);}public get2DLine(i:number):[Vector3Dd,Vector3Dd]{const p=i*8;return [new Vector3Dd(this.lines[p]!,this.lines[p+1]!,0),new Vector3Dd(this.lines[p+2]!,this.lines[p+3]!,0)];}public getNumLines():number{return this.lines.length/8;}public exportRgbImage(out:RGBImageUncompressed):void{const p=new RGBPixel();p.r=p.g=p.b=-1;for(let n=0;n<this.getNumLines();n++){const [a,b]=this.get2DLine(n);this.draw(out,Math.trunc((out.getXSize()-1)*(a.x()+1)/2),Math.trunc((out.getYSize()-1)*(1-(a.y()+1)/2)),Math.trunc((out.getXSize()-1)*(b.x()+1)/2),Math.trunc((out.getYSize()-1)*(1-(b.y()+1)/2)),p);}}private draw(o:RGBImageUncompressed,x0:number,y0:number,x1:number,y1:number,p:RGBPixel):void{const dx=Math.abs(x1-x0),sx=x0<x1?1:-1,dy=-Math.abs(y1-y0),sy=y0<y1?1:-1;for(;;){if(x0>=0&&y0>=0&&x0<o.getXSize()&&y0<o.getYSize())o.putPixel(x0,y0,p);if(x0===x1&&y0===y1)break;const e=2*(dx+dy);if(e>=dy){x0+=sx;}if(e<=dx){y0+=sy;}}} }
