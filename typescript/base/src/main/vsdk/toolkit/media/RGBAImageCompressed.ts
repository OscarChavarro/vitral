import { Image } from "./Image.js";import { RGBPixel } from "./RGBPixel.js";import { RGBAPixel } from "./RGBAPixel.js";import { UnsupportedOperationException } from "../../../java/lang/UnsupportedOperationException.js";
export class RGBAImageCompressed extends Image {public static readonly COMPRESSION_UNKNOWN=0;public static readonly COMPRESSION_DXT1=1;public static readonly COMPRESSION_DXT3=3;public static readonly COMPRESSION_DXT5=5;private data:Int8Array|null=null;private xSize=0;private ySize=0;private format=0;private compressedSize=0;public dettach():void{this.data=null;}public init(w:number,h:number):boolean{this.xSize=w;this.ySize=h;this.format=0;this.compressedSize=0;this.data=new Int8Array();return true;}public initNoFill(w:number,h:number):boolean{return this.init(w,h);}public initCompressed(w:number,h:number,format:number,data:Int8Array|Uint8Array|null):boolean{if(w<=0||h<=0||data===null)return false;const size=RGBAImageCompressed.calculateTopLevelDataSize(w,h,format);if(data.length<size)return false;this.xSize=w;this.ySize=h;this.format=format;this.compressedSize=size;this.data=new Int8Array(data.buffer.slice(data.byteOffset,data.byteOffset+data.byteLength));return true;}public setRawImage(w:number,h:number,f:number,d:Int8Array|Uint8Array):void{this.initCompressed(w,h,f,d);}public getCompressionFormat():number{return this.format;}public getCompressedDataSize():number{return this.compressedSize;}public getRawImage():Int8Array|null{return this.data;}public getRawImageDirectBuffer():Int8Array|null{return this.data;}public getXSize():number{return this.xSize;}public getYSize():number{return this.ySize;}public override getSizeInBytes():number{return (this.data?.length??0)+24;}private unsupported():never{throw new UnsupportedOperationException("Pixel access is unavailable for compressed image blocks");}public putPixel(_x:number,_y:number,_r:number|RGBAPixel,_g?:number,_b?:number,_a?:number):void{this.unsupported();}public putPixelRgb(_x:number,_y:number,_p:RGBPixel):void{this.unsupported();}public getPixel(_x:number,_y:number):RGBAPixel{return this.unsupported();}public getPixelRgb(_x:number,_y:number):RGBPixel;public getPixelRgb(_x:number,_y:number,_p:RGBPixel):void;public getPixelRgb(_x:number,_y:number,_p?:RGBPixel):RGBPixel|void{return this.unsupported();}public static calculateTopLevelDataSize(w:number,h:number,f:number):number{const blocks=Math.ceil(w/4)*Math.ceil(h/4);if(f===this.COMPRESSION_DXT1)return blocks*8;if(f===this.COMPRESSION_DXT3||f===this.COMPRESSION_DXT5)return blocks*16;return 0;} }
