{
    "name": "vitral",
    "version": "1.3.0",
    "private": true,
    "workspaces": [
        "base",
        "fs"
    ],
    "scripts": {
        "clean": "./scripts/clean.sh",
        "build": "npm run --workspace @vitral/base build && npm run --workspace @vitral/fs build",
        "build:fs": "npm run build && npm run --workspace @vitral/fs build",
        "api-test:concurrency": "npm run build && npm run build --prefix testsuite/_APITests/ConcurrencyWorkersForBrowsers && npm run build --prefix testsuite/_APITests/ConcurrencyFibers",
        "test": "npm run --workspace @vitral/base test",
        "pack": "npm run --workspace @vitral/base pack",
        "pack:fs": "npm run --workspace @vitral/fs pack",
        "lint": "prettier --check . && eslint .",
        "lint-fix": "prettier --write . && eslint . --fix",
        "verify": "npm run clean && npm run build && npm run test && ./scripts/compile.sh",
        "clean_all": "./scripts/clean.sh"
    },
    "devDependencies": {
        "@types/node": "^22.15.3",
        "eslint": "^9.36.0",
        "eslint-config-prettier": "^10.1.8",
        "prettier": "^3.6.2",
        "typescript-eslint": "^8.44.1",
        "typescript": "^5.8.3",
        "vitest": "^3.1.2"
    },
    "engines": {
        "node": ">=22"
    }
}
