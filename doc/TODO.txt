  - Check that RGBImage has same services than RGBAImage (i.e. renderers in
    Jogl and Awt)
  - Define HDRI interpretation of values in the 
    vitral.toolkits.common.ColorRgb class
  - Check the names in JoglRenderers... should be activate and draw,
    or activateGL and drawGL, but not mixed ...
  - Rename drawGL and activateGL to draw and activate in jogl...
  - The doIntersection method in RayableObject is not taking into account
    the scale factors yet...
  - TranslationGizmo has one error: when interacting with axes in a camera
    who's front vector is aligned with j vector...
  - Persistence schema for curves and patches are not handling correctly
    the exceptions when requested file does not exist. Should not cause
    the program to die (check SceneEditorApplication)
  - ZBuffer internal representation should be based on doubles, not floats
  - Check tga format import functionality
  - Implement primitive and intersection accounting on render operations,
    using VSDK class
  - Rename QualitySelection class to RenderConfiguration, and include
    managing of user interaction level colors
  - Implement intersection operation for parametric bi-cubic patches
  - Change all object.getClass().getName().equals tests to object instanceof
    class ...
  - To complete the implementation of IndexedColorImage (getpixel/setpixel
    methods for RGB version)
  - In SceneEditorApplication, keyPressed event is not processing correctly:
    only accepts 2nd key after some other event as mouse click or cursor
    re-entry to working canvas window
  - Take into account for the mouse processing inside gizmos, that eventually
    and application can have a non (0, 0) located viewport.  For that cases,
    gizmos should include a `setViewportPosition` method, and act acordingly.
  - Explain the failures in tangent vector interpretation for the raytracing
    bump-mapping algorithm!
  - Check when textures are great (more than 1024 in X or Y extend): when
    working with Linux or Nvidia Cg, they work fine, but they do not work
    on fixed function OpenGL on Windows.
  - Implement OBHU technique in SearchEngineFor3DModels
  - Color arrays are handled on JoglTriangleMeshRenderer only for the
    vertex arrays case. Normal processing of color array with glColor3d
    primitive pending.
